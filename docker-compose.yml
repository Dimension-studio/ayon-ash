services:
  worker:
    build: .
    image: ynput/ayon-ash:latest
    container_name: ayon-ash
    hostname: local-worker
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
